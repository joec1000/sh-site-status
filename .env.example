# ---- API (apps/api) ----
PORT=8080
GCS_BUCKET=your-status-page-bucket
ADMIN_KEY=change-me-to-a-strong-secret
CORS_ORIGIN=https://status.yourdomain.com

# ---- Web (apps/web) ----
# Set to API URL for production builds (leave empty for dev proxy)
VITE_API_URL=https://api-status.yourdomain.com
